//
//  ConferenceImage.swift
//  RubyConferences
//
//  Created by Rashmi Yadav on 5/2/15.
//
//

import Foundation
import UIKit

struct ConferenceImage{
   /* var imageButton = UIButton(frame: CGRectMake(10, yDistance + 20 , 40, 40))
    var image_url = NSURL(string: conferenceInfo.image_url)
    var data = NSData(contentsOfURL: image_url!)
    var conferenceImage: UIImage? = UIImage(data: data!)
    
    
    imageButton.setTitleColor(UIColor.clearColor(), forState: .Normal)
    imageButton.addTarget(self, action: "pressed:", forControlEvents: .TouchUpInside)
    
    imageButton.setBackgroundImage(conferenceImage!, forState: .Normal)
    
    var titleWidth = self.view.bounds.size.width - imageButton.bounds.size.width - 100
    
    var labelTitle = UILabel(frame: CGRectMake(imageButton.bounds.size.width + 20, imageButton.frame.origin.y, titleWidth, 60))
    
    
    labelTitle.textAlignment = NSTextAlignment.Left
    labelTitle.numberOfLines = 0
    labelTitle.font = UIFont(name: "Helvetica Neue", size: 15.0)
    labelTitle.text = "\(conferenceInfo.name.capitalizedString)"
    labelTitle.sizeToFit()*/

    
}
